kind: ConfigMap
apiVersion: v1
metadata:
  name: configmap-example-api
data:
  # Configuration values can be set as key-value properties
  database: mongodb
  database_uri: mongodb://localhost:27017
  TEMP_DIR: temp/usr/app